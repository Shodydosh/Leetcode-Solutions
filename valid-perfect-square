class Solution {
public:
    bool isPerfectSquare(int num) {
        if(num < 0) return false;
        
        long long p = 1;
        while(p*p < num) p++;
        
        return p*p == num;
    }
};
